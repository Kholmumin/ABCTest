Hi, There are two branches where you can review the test assignment:
- feature/UIKit – contains the implementation of the task using UIKit.
- feature/SwiftUI – contains the implementation of the task using SwiftUI.
